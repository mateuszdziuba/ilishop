---
phase: 02-shared-kanva-primitives
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - sections/kanva-newsletter.liquid
  - sections/kanva-image-grid.liquid
  - sections/media-with-content.liquid
autonomous: true
requirements: [EDIT-01, EDIT-04, COMP-01, COMP-03]
must_haves:
  truths:
    - "Merchant can add a Newsletter section to any page template body from the theme editor"
    - "Merchant can add an Image Grid section to any page template from the theme editor"
    - "Newsletter form submits through Shopify native customer form and creates a customer tagged with newsletter"
    - "Image grid renders merchant-configurable static images in a responsive CSS grid"
    - "media-with-content section can be used for Kanva story/journey rows through a new preset without breaking existing presets"
    - "Shopper sees consistent Kanva heading and card treatments across the new sections via shared snippets"
  artifacts:
    - path: "sections/kanva-newsletter.liquid"
      provides: "Standalone newsletter email capture section"
      contains: "form 'customer'"
    - path: "sections/kanva-image-grid.liquid"
      provides: "Static image grid section for Instagram-style content"
      contains: "kanva-image-grid"
    - path: "sections/media-with-content.liquid"
      provides: "Adapted section with Kanva story row preset"
      contains: "Kanva Story Row"
  key_links:
    - from: "sections/kanva-newsletter.liquid"
      to: "snippets/kanva-heading.liquid"
      via: "render tag"
      pattern: "render 'kanva-heading'"
    - from: "sections/kanva-newsletter.liquid"
      to: "Shopify customer form"
      via: "form tag"
      pattern: "form 'customer'"
    - from: "sections/kanva-image-grid.liquid"
      to: "snippets/kanva-card.liquid"
      via: "render tag"
      pattern: "render 'kanva-card'"
    - from: "sections/kanva-image-grid.liquid"
      to: "snippets/kanva-heading.liquid"
      via: "render tag"
      pattern: "render 'kanva-heading'"
    - from: "sections/media-with-content.liquid"
      to: "sections/media-with-content.liquid (schema)"
      via: "new preset entry"
      pattern: "Kanva Story Row"
---

<objective>
Create the Newsletter and Image Grid standalone sections, and adapt `media-with-content.liquid` with a Kanva preset — completing the full set of reusable Kanva editorial primitives.

Purpose: Delivers the final two new sections (COMP-01) and the Horizon section adaptation (COMP-03, D-05/D-07/D-08). After this plan, Phase 2's full reusable section inventory is complete and ready for page assembly in Phase 3+. Per D-04, D-05, D-06, D-07, D-08.

Output: Two new section files in `sections/`, one modified section file with new preset.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-shared-kanva-primitives/02-CONTEXT.md
@.planning/phases/02-shared-kanva-primitives/02-RESEARCH.md
@.planning/phases/01-stabilization-and-editor-contract/01-responsive-editor-contract.md
@.planning/phases/02-shared-kanva-primitives/02-01-SUMMARY.md

<interfaces>
<!-- Shared snippets from Plan 01 -->

From snippets/kanva-heading.liquid:
- Render: {% render 'kanva-heading', label: '...', heading: '...', subtext: '...', heading_level: 'h2', alignment: 'center' %}
- CSS classes: .kanva-heading, .kanva-heading__label, .kanva-heading__title, .kanva-heading__subtext

From snippets/kanva-card.liquid:
- Render: {% render 'kanva-card', image: block.settings.image, image_alt: block.settings.alt, url: block.settings.url, badge_text: '', title: '', subtitle: '' %}
- CSS classes: .kanva-card, .kanva-card__media, .kanva-card__img (with hover-zoom)

From snippets/kanva-badge.liquid:
- Render: {% render 'kanva-badge', text: '...' %}

From snippets/spacing-style.liquid:
- Render: {% render 'spacing-style', settings: section.settings %}
- Requires: padding-block-start, padding-block-end range settings in section schema

From snippets/theme-styles-variables.liquid (Kanva tokens):
- --kanva-cream: #F5F3EF; --kanva-sage: #8B9E6E; --kanva-border: #E8E4DF
- --kanva-text-primary: #1A1A1A; --kanva-text-muted: #6B6B6B
- --kanva-card-radius: 10px; --kanva-section-pad: 80px

Horizon section wrapper pattern:
```liquid
<div class="section-background color-{{ section.settings.color_scheme }}"></div>
<div class="section section--{{ section.settings.section_width }} color-{{ section.settings.color_scheme }} spacing-style ..."
     style="{% render 'spacing-style', settings: section.settings %}">
```

Shopify customer form pattern (from Horizon footer):
```liquid
{% form 'customer', id: 'newsletter-' | append: section.id %}
  <input type="hidden" name="contact[tags]" value="newsletter">
  <input type="email" name="contact[email]" required>
  <button type="submit">Subscribe</button>
{% endform %}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create kanva-newsletter.liquid section</name>
  <files>sections/kanva-newsletter.liquid</files>
  <read_first>
    - snippets/kanva-heading.liquid (verify render interface)
    - snippets/spacing-style.liquid (parameter interface)
    - theme-to-clone/kanva-components.html (lines 254-274 — newsletter HTML structure: heading, subtext, form, image)
    - theme-to-clone/kanva-docs.md (Design System — button and form styling)
    - sections/media-with-content.liquid (lines 1-40 — Horizon section wrapper pattern)
    - .planning/phases/02-shared-kanva-primitives/02-RESEARCH.md (Pattern 5 — newsletter with native Shopify form, Pitfall 6 — use 'customer' not 'contact')
  </read_first>
  <action>
    Create `sections/kanva-newsletter.liquid` — a standalone email capture section for page body use.

    **Liquid body structure:**
    1. `<div class="section-background color-{{ section.settings.color_scheme }}"></div>`
    2. Main wrapper: `<div class="section section--{{ section.settings.section_width }} color-{{ section.settings.color_scheme }} spacing-style kanva-newsletter" style="{% render 'spacing-style', settings: section.settings %}" data-testid="kanva-newsletter">`
    3. Inner layout: `<div class="kanva-newsletter__inner">` with two children:
       - Content side: heading + form
       - Image side: optional decorative image

    **Content side:**
    ```liquid
    <div class="kanva-newsletter__content">
      {% render 'kanva-heading',
        label: section.settings.label,
        heading: section.settings.heading,
        subtext: section.settings.subtext,
        heading_level: 'h2'
      %}
      {%- comment -%} CRITICAL: Use 'customer' form, NOT 'contact' — see Pitfall 6 {%- endcomment -%}
      {% form 'customer', id: 'kanva-newsletter-' | append: section.id %}
        {%- if form.posted_successfully? -%}
          <p class="kanva-newsletter__success">{{ section.settings.success_message | default: 'Thank you for subscribing!' }}</p>
        {%- else -%}
          {%- if form.errors -%}
            <p class="kanva-newsletter__error">{{ form.errors.translated_fields['email'] | default: 'Please enter a valid email.' }}</p>
          {%- endif -%}
          <input type="hidden" name="contact[tags]" value="newsletter">
          <div class="kanva-newsletter__form">
            <input
              type="email"
              name="contact[email]"
              id="kanva-newsletter-email-{{ section.id }}"
              placeholder="{{ section.settings.email_placeholder | default: 'Your email address' }}"
              required
              class="kanva-newsletter__input"
              autocomplete="email"
            >
            <button type="submit" class="kanva-newsletter__btn">
              {{ section.settings.button_label | default: 'Subscribe' }}
            </button>
          </div>
        {%- endif -%}
      {% endform %}
    </div>
    ```

    **Image side:**
    ```liquid
    {%- if section.settings.image != blank -%}
      <div class="kanva-newsletter__image">
        {{ section.settings.image | image_url: width: 600 | image_tag: alt: section.settings.image.alt, loading: 'lazy', class: 'kanva-newsletter__img' }}
      </div>
    {%- endif -%}
    ```

    **Section schema:**
    ```json
    {
      "name": "Kanva Newsletter",
      "tag": "section",
      "disabled_on": { "groups": ["header"] },
      "settings": [
        { "type": "color_scheme", "id": "color_scheme", "label": "Color scheme", "default": "scheme-3" },
        { "type": "select", "id": "section_width", "label": "Section width", "options": [
          { "value": "page-width", "label": "Page width" },
          { "value": "full-width", "label": "Full width" }
        ], "default": "page-width" },
        { "type": "text", "id": "label", "label": "Section label" },
        { "type": "text", "id": "heading", "label": "Heading", "default": "Stay Updated, Stay Radiant" },
        { "type": "textarea", "id": "subtext", "label": "Subtext", "default": "Get skincare tips, new arrivals, and exclusive offers delivered to your inbox." },
        { "type": "text", "id": "email_placeholder", "label": "Email placeholder", "default": "Your email address" },
        { "type": "text", "id": "button_label", "label": "Button label", "default": "Subscribe" },
        { "type": "text", "id": "success_message", "label": "Success message", "default": "Thank you for subscribing!" },
        { "type": "image_picker", "id": "image", "label": "Decorative image" },
        { "type": "header", "content": "Padding" },
        { "type": "range", "id": "padding-block-start", "label": "Top", "min": 0, "max": 120, "step": 4, "unit": "px", "default": 80 },
        { "type": "range", "id": "padding-block-end", "label": "Bottom", "min": 0, "max": 120, "step": 4, "unit": "px", "default": 80 }
      ],
      "blocks": [],
      "presets": [
        {
          "name": "Kanva Newsletter",
          "category": "Kanva Editorial"
        }
      ]
    }
    ```

    **`{% stylesheet %}` block:**
    - `.kanva-newsletter__inner`: display grid, grid-template-columns 1fr 1fr, gap 48px, align-items center
    - `.kanva-newsletter__form`: display flex, gap 0, margin-top 24px
    - `.kanva-newsletter__input`: flex 1, padding 14px 20px, border 1px solid var(--kanva-border), border-radius var(--kanva-btn-radius) 0 0 var(--kanva-btn-radius), font-size 15px, outline none, background #fff
    - `.kanva-newsletter__input:focus`: border-color var(--kanva-text-primary)
    - `.kanva-newsletter__btn`: padding 14px 28px, background var(--kanva-text-primary), color #fff, border 0, border-radius 0 var(--kanva-btn-radius) var(--kanva-btn-radius) 0, font-size 14px, font-weight 500, cursor pointer, white-space nowrap
    - `.kanva-newsletter__btn:hover`: opacity 0.85
    - `.kanva-newsletter__img`: width 100%, border-radius var(--kanva-card-radius), object-fit cover, max-height 400px
    - `.kanva-newsletter__success`: color var(--kanva-sage), font-weight 500, margin-top 16px
    - `.kanva-newsletter__error`: color #c0392b, font-size 14px, margin-bottom 8px
    - `@media screen and (max-width: 749px)`: `.kanva-newsletter__inner` grid-template-columns 1fr (stack), `.kanva-newsletter__image` order -1 (image first on mobile)
  </action>
  <verify>
    <automated>cd /Users/mati/Mine/horizon && test -f sections/kanva-newsletter.liquid && grep -q "form 'customer'" sections/kanva-newsletter.liquid && grep -q "contact\[tags\]" sections/kanva-newsletter.liquid && grep -q "newsletter" sections/kanva-newsletter.liquid && grep -q "disabled_on" sections/kanva-newsletter.liquid && grep -q "render 'kanva-heading'" sections/kanva-newsletter.liquid && grep -q "render 'spacing-style'" sections/kanva-newsletter.liquid && grep -q "form.posted_successfully" sections/kanva-newsletter.liquid && sh scripts/phase1-regression-gate.sh && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - sections/kanva-newsletter.liquid exists
    - File contains `form 'customer'` (NOT `form 'contact'` — per Pitfall 6)
    - File contains `name="contact[tags]" value="newsletter"` hidden input
    - File contains `form.posted_successfully?` success state handling
    - File contains `form.errors` error state handling
    - File contains `render 'kanva-heading'` (uses shared heading snippet)
    - File contains `render 'spacing-style', settings: section.settings`
    - File contains `"disabled_on": { "groups": ["header"] }` in schema
    - File contains `"category": "Kanva Editorial"` in presets
    - File contains `"id": "image"` image_picker setting for decorative image
    - File contains `"id": "email_placeholder"` and `"id": "button_label"` settings
    - File contains `data-testid="kanva-newsletter"`
    - File contains `autocomplete="email"` on the email input
    - `cd /Users/mati/Mine/horizon && sh scripts/phase1-regression-gate.sh` exits 0
  </acceptance_criteria>
  <done>Newsletter section exists with Shopify native customer form, email capture, success/error states, optional decorative image, Kanva heading integration, and responsive layout. Regression gate passes.</done>
</task>

<task type="auto">
  <name>Task 2: Create kanva-image-grid.liquid section</name>
  <files>sections/kanva-image-grid.liquid</files>
  <read_first>
    - snippets/kanva-heading.liquid (verify render interface)
    - snippets/kanva-card.liquid (verify render interface — used for each grid item)
    - snippets/spacing-style.liquid (parameter interface)
    - theme-to-clone/kanva-components.html (lines 277-293 — Instagram grid HTML structure: 6-item grid with square images and links)
    - .planning/phases/02-shared-kanva-primitives/02-RESEARCH.md (Pattern 6 — image grid with static image blocks, Pitfall 2 — explicit block types)
  </read_first>
  <action>
    Create `sections/kanva-image-grid.liquid` — a static image grid for Instagram-style content. v1 uses static images (not live API — per Out of Scope).

    **Liquid body structure:**
    1. `<div class="section-background color-{{ section.settings.color_scheme }}"></div>`
    2. Main wrapper: `<div class="section section--{{ section.settings.section_width }} color-{{ section.settings.color_scheme }} spacing-style kanva-image-grid" style="{% render 'spacing-style', settings: section.settings %}" data-testid="kanva-image-grid">`
    3. Header row: `<div class="kanva-image-grid__header">` containing:
       - Left: `{% render 'kanva-heading', label: section.settings.label, heading: section.settings.heading %}`
       - Right (optional): `{%- if section.settings.link_text != blank -%}<a href="{{ section.settings.link_url }}" class="kanva-image-grid__link">{{ section.settings.link_text }}</a>{%- endif -%}`
    4. Grid: `<div class="kanva-image-grid__grid">` containing `{%- content_for 'blocks' -%}`

    **Block type `kanva-image-grid-item`:**

    Each block renders the image using the shared `kanva-card` snippet:
    ```liquid
    <div class="kanva-image-grid__item" {{ block.shopify_attributes }}>
      {% render 'kanva-card',
        image: block.settings.image,
        image_alt: block.settings.alt,
        url: block.settings.url
      %}
    </div>
    ```

    **Block schema settings (per research Pattern 6):**
    - `image` — image_picker, label "Image"
    - `url` — url, label "Link"
    - `alt` — text, label "Alt text"

    **Section schema:**
    ```json
    {
      "name": "Kanva Image Grid",
      "tag": "section",
      "disabled_on": { "groups": ["header"] },
      "settings": [
        { "type": "color_scheme", "id": "color_scheme", "label": "Color scheme", "default": "scheme-1" },
        { "type": "select", "id": "section_width", "label": "Section width", "options": [
          { "value": "page-width", "label": "Page width" },
          { "value": "full-width", "label": "Full width" }
        ], "default": "page-width" },
        { "type": "text", "id": "label", "label": "Section label", "default": "Follow us on Instagram" },
        { "type": "text", "id": "heading", "label": "Section heading" },
        { "type": "text", "id": "link_text", "label": "Link text", "default": "@kanva" },
        { "type": "url", "id": "link_url", "label": "Link URL" },
        { "type": "header", "content": "Padding" },
        { "type": "range", "id": "padding-block-start", "label": "Top", "min": 0, "max": 120, "step": 4, "unit": "px", "default": 80 },
        { "type": "range", "id": "padding-block-end", "label": "Bottom", "min": 0, "max": 120, "step": 4, "unit": "px", "default": 80 }
      ],
      "blocks": [
        { "type": "kanva-image-grid-item", "name": "Image" }
      ],
      "presets": [
        {
          "name": "Kanva Image Grid",
          "category": "Kanva Editorial",
          "blocks": [
            { "type": "kanva-image-grid-item" },
            { "type": "kanva-image-grid-item" },
            { "type": "kanva-image-grid-item" },
            { "type": "kanva-image-grid-item" },
            { "type": "kanva-image-grid-item" },
            { "type": "kanva-image-grid-item" }
          ]
        }
      ]
    }
    ```

    **`{% stylesheet %}` block:**
    - `.kanva-image-grid__header`: display flex, justify-content space-between, align-items flex-end, margin-bottom 32px, flex-wrap wrap, gap 16px
    - `.kanva-image-grid__link`: font-size 14px, font-weight 500, color var(--kanva-text-primary), text-decoration none, border-bottom 1px solid currentColor
    - `.kanva-image-grid__link:hover`: opacity 0.7
    - `.kanva-image-grid__grid`: display grid, grid-template-columns repeat(auto-fill, minmax(180px, 1fr)), gap 4px
    - `.kanva-image-grid__item`: aspect-ratio 1 / 1
    - `@media screen and (max-width: 749px)`: `.kanva-image-grid__grid` grid-template-columns repeat(3, 1fr)
    - `@media screen and (max-width: 449px)`: `.kanva-image-grid__grid` grid-template-columns repeat(2, 1fr)
  </action>
  <verify>
    <automated>cd /Users/mati/Mine/horizon && test -f sections/kanva-image-grid.liquid && grep -q "kanva-image-grid" sections/kanva-image-grid.liquid && grep -q "kanva-image-grid-item" sections/kanva-image-grid.liquid && grep -q "disabled_on" sections/kanva-image-grid.liquid && grep -q "render 'kanva-heading'" sections/kanva-image-grid.liquid && grep -q "render 'kanva-card'" sections/kanva-image-grid.liquid && grep -q "render 'spacing-style'" sections/kanva-image-grid.liquid && sh scripts/phase1-regression-gate.sh && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - sections/kanva-image-grid.liquid exists
    - File contains `render 'kanva-heading'` (uses shared heading snippet)
    - File contains `render 'kanva-card'` (uses shared card snippet for each grid item)
    - File contains `render 'spacing-style', settings: section.settings`
    - File contains `"disabled_on": { "groups": ["header"] }` in schema
    - File contains `"type": "kanva-image-grid-item"` block definition (NOT `@theme`)
    - File contains `"category": "Kanva Editorial"` in presets
    - File contains `"id": "link_text"` and `"id": "link_url"` settings for the optional header link
    - File contains 6 default blocks in the preset
    - File contains `data-testid="kanva-image-grid"`
    - File contains responsive grid CSS with `repeat(auto-fill, minmax(180px, 1fr))`
    - `cd /Users/mati/Mine/horizon && sh scripts/phase1-regression-gate.sh` exits 0
  </acceptance_criteria>
  <done>Image grid section exists with block-based static images, Kanva card snippet integration, responsive grid layout, optional header link, and 6-item default preset. Regression gate passes.</done>
</task>

<task type="auto">
  <name>Task 3: Adapt media-with-content.liquid with Kanva story row preset</name>
  <files>sections/media-with-content.liquid</files>
  <read_first>
    - sections/media-with-content.liquid (FULL file — understand all existing presets, schema IDs, and structure before making any change)
    - .planning/phases/02-shared-kanva-primitives/02-CONTEXT.md (D-05, D-07, D-08 — adapt existing, do NOT rename or reorganize schema IDs)
    - .planning/phases/02-shared-kanva-primitives/02-RESEARCH.md (Pattern 4 — adaptation approach, Pitfall 3 — do NOT rename or remove existing presets, Anti-patterns)
    - .planning/phases/01-stabilization-and-editor-contract/01-responsive-editor-contract.md (section 3 — canonical setting IDs)
  </read_first>
  <action>
    Adapt `sections/media-with-content.liquid` for Kanva story/journey rows per D-05, D-07, D-08. This is a MINIMAL change — only additions, no modifications to existing code.

    **CRITICAL CONSTRAINTS (D-08):**
    - Do NOT rename any existing schema IDs
    - Do NOT remove or reorder existing presets
    - Do NOT modify existing CSS rules
    - Do NOT change the section `name` in the schema

    **Changes (all additive):**

    1. **Add a new preset** to the `presets` array in the `{% schema %}` block. Append it AFTER all existing presets (do not insert before or between existing ones):

    ```json
    {
      "name": "Kanva Story Row",
      "category": "Kanva Editorial",
      "settings": {
        "color_scheme": "scheme-3",
        "media_width": "50",
        "media_height": "auto",
        "section_width": "page-width",
        "padding-block-start": 80,
        "padding-block-end": 80
      }
    }
    ```

    2. **Add a `kanva-split` CSS modifier class** to the section's `{% stylesheet %}` block (append at the end, after all existing rules):

    ```css
    /* Kanva story row treatment (Phase 2) */
    .media-with-content.kanva-split .media-with-content__content {
      padding-block: 40px;
    }
    .media-with-content.kanva-split .media-with-content__media img,
    .media-with-content.kanva-split .media-with-content__media video {
      border-radius: var(--kanva-card-radius, 10px);
    }
    ```

    3. **Add the `kanva-split` class conditionally** in the Liquid body. Find the main wrapper div that has `class="section section--{{ section.settings.section_width }} ... media-with-content ..."` and add a conditional class. Specifically, AFTER the existing `{{ media_class_modifier }}` output on the class attribute, add:
    ```liquid
    {%- if section.settings.color_scheme == 'scheme-3' %} kanva-split{%- endif -%}
    ```

    This applies the Kanva border-radius treatment when the warm cream color scheme is selected (the default for the Kanva Story Row preset). It is a heuristic — a simple CSS class addition that does not break any existing behavior because `kanva-split` is only used by the new CSS rules appended above.

    **No other changes.** The existing section already has `media_position`, `media_width`, `media_height`, `section_width`, `extend_media`, and `color_scheme` settings that cover all layout needs for Kanva story rows. The Kanva heading treatment inside the content blocks will be composed in Phase 5 (about page assembly) using the section's existing block system.
  </action>
  <verify>
    <automated>cd /Users/mati/Mine/horizon && grep -q "Kanva Story Row" sections/media-with-content.liquid && grep -q "kanva-split" sections/media-with-content.liquid && grep -q "kanva-card-radius" sections/media-with-content.liquid && grep -q "Kanva Editorial" sections/media-with-content.liquid && sh scripts/phase1-regression-gate.sh && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - sections/media-with-content.liquid contains `"name": "Kanva Story Row"` preset
    - sections/media-with-content.liquid contains `"category": "Kanva Editorial"` in the new preset
    - sections/media-with-content.liquid contains `.kanva-split` CSS rules in the stylesheet block
    - sections/media-with-content.liquid contains `var(--kanva-card-radius` in the new CSS
    - All existing presets in the schema are unchanged (verify by confirming existing preset names still present: "Editorial" or similar)
    - No existing schema IDs have been renamed or removed
    - The `kanva-split` class is conditionally added based on color scheme
    - `cd /Users/mati/Mine/horizon && sh scripts/phase1-regression-gate.sh` exits 0
  </acceptance_criteria>
  <done>media-with-content section has a new "Kanva Story Row" preset with warm cream color scheme defaults and border-radius treatment, without modifying any existing schema IDs, presets, or CSS rules. Regression gate passes.</done>
</task>

</tasks>

<verification>
1. All three section files exist / are modified
2. Newsletter uses `{% form 'customer' %}` (not `{% form 'contact' %}`)
3. Image grid uses `{% render 'kanva-card' %}` for each item
4. media-with-content has new "Kanva Story Row" preset appended after existing presets
5. All new sections have `disabled_on: { groups: ["header"] }`
6. No existing schema IDs in media-with-content have been renamed or removed
7. `cd /Users/mati/Mine/horizon && sh scripts/phase1-regression-gate.sh` exits 0
</verification>

<success_criteria>
- Newsletter section captures email through Shopify native customer form with success/error states
- Image grid section renders responsive static image grid with shared card snippet
- media-with-content section has Kanva Story Row preset without breaking existing presets or schema
- All three sections integrate with Kanva heading/spacing/token infrastructure from Plan 01
- Phase 1 regression gate passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/02-shared-kanva-primitives/02-03-SUMMARY.md`
</output>
