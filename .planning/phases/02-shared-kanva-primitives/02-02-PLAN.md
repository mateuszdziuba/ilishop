---
phase: 02-shared-kanva-primitives
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - sections/kanva-feature-strip.liquid
  - sections/kanva-testimonial.liquid
autonomous: true
requirements: [EDIT-01, EDIT-04, COMP-01]
must_haves:
  truths:
    - "Merchant can add a Feature Strip section to any page template from the Shopify theme editor"
    - "Merchant can add, reorder, duplicate, and remove Feature Strip item blocks without code changes"
    - "Merchant can add a Testimonial section to any page template from the Shopify theme editor"
    - "Merchant can add multiple testimonial quote blocks and configure star rating per quote"
    - "Shopper sees consistent Kanva heading treatment in both sections via the shared kanva-heading snippet"
  artifacts:
    - path: "sections/kanva-feature-strip.liquid"
      provides: "4-column icon+label trust bar section"
      contains: "kanva-feature-strip"
    - path: "sections/kanva-testimonial.liquid"
      provides: "Centered quote testimonial section"
      contains: "kanva-testimonial"
  key_links:
    - from: "sections/kanva-feature-strip.liquid"
      to: "snippets/kanva-heading.liquid"
      via: "render tag"
      pattern: "render 'kanva-heading'"
    - from: "sections/kanva-testimonial.liquid"
      to: "snippets/kanva-heading.liquid"
      via: "render tag"
      pattern: "render 'kanva-heading'"
    - from: "sections/kanva-feature-strip.liquid"
      to: "snippets/spacing-style.liquid"
      via: "render tag"
      pattern: "render 'spacing-style'"
    - from: "sections/kanva-testimonial.liquid"
      to: "snippets/spacing-style.liquid"
      via: "render tag"
      pattern: "render 'spacing-style'"
---

<objective>
Create the Feature Strip and Testimonial standalone Kanva sections — the two editorial patterns that have no existing Horizon equivalent.

Purpose: Delivers two of the four new reusable sections required by COMP-01. Both sections use the shared heading and spacing infrastructure from Plan 01. Per D-04 (standalone section per editorial pattern), D-06 (responsive editor contract), D-07 (new sections where no Horizon equivalent exists).

Output: Two new section files in `sections/`, each with `{% schema %}`, `{% stylesheet %}`, block types, and presets.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-shared-kanva-primitives/02-CONTEXT.md
@.planning/phases/02-shared-kanva-primitives/02-RESEARCH.md
@.planning/phases/01-stabilization-and-editor-contract/01-responsive-editor-contract.md
@.planning/phases/02-shared-kanva-primitives/02-01-SUMMARY.md

<interfaces>
<!-- Shared snippets from Plan 01 that this plan consumes -->

From snippets/kanva-heading.liquid:
- Render: {% render 'kanva-heading', label: '...', heading: '...', subtext: '...', heading_level: 'h2', alignment: 'center' %}
- CSS classes output: .kanva-heading, .kanva-heading__label, .kanva-heading__title, .kanva-heading__subtext

From snippets/kanva-badge.liquid:
- Render: {% render 'kanva-badge', text: '...' %}
- CSS classes output: .kanva-badge

From snippets/spacing-style.liquid:
- Render: {% render 'spacing-style', settings: section.settings %}
- Requires schema settings: padding-block-start (range), padding-block-end (range)

From snippets/theme-styles-variables.liquid (Kanva tokens):
- --kanva-cream: #F5F3EF
- --kanva-sage: #8B9E6E
- --kanva-border: #E8E4DF
- --kanva-text-primary: #1A1A1A
- --kanva-text-muted: #6B6B6B
- --kanva-section-pad: 80px

Horizon section pattern (from sections/media-with-content.liquid lines 13-18):
```liquid
<div class="section-background color-{{ section.settings.color_scheme }}"></div>
<div
  class="section section--{{ section.settings.section_width }} color-{{ section.settings.color_scheme }} spacing-style ..."
  style="{% render 'spacing-style', settings: section.settings %}"
>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create kanva-feature-strip.liquid section</name>
  <files>sections/kanva-feature-strip.liquid</files>
  <read_first>
    - snippets/kanva-heading.liquid (verify render interface from Plan 01)
    - snippets/spacing-style.liquid (parameter interface for padding)
    - theme-to-clone/kanva-components.html (lines 122-164 — feature strip HTML structure with 4 SVG icons)
    - theme-to-clone/kanva-docs.md (Design System section — spacing, typography values)
    - sections/media-with-content.liquid (lines 1-40 — Horizon section wrapper pattern: section-background div, color scheme class, spacing-style render)
    - .planning/phases/02-shared-kanva-primitives/02-RESEARCH.md (Pattern 1 — section anatomy, Feature Strip Block Schema, Anti-patterns)
    - .planning/phases/01-stabilization-and-editor-contract/01-responsive-editor-contract.md (section 3 — canonical setting IDs for padding)
  </read_first>
  <action>
    Create `sections/kanva-feature-strip.liquid` — a 4-column icon + label trust/value bar section.

    **Liquid body structure:**
    1. `<div class="section-background color-{{ section.settings.color_scheme }}"></div>` — Horizon color scheme background
    2. Main wrapper: `<div class="section section--{{ section.settings.section_width }} color-{{ section.settings.color_scheme }} spacing-style kanva-feature-strip" style="{% render 'spacing-style', settings: section.settings %}" data-testid="kanva-feature-strip">`
    3. Optional heading via `{% render 'kanva-heading', label: section.settings.label, heading: section.settings.heading, alignment: 'center' %}`
    4. Feature grid: `<div class="kanva-feature-strip__grid">` containing `{%- content_for 'blocks' -%}`

    **Block type `kanva-feature-item`:**

    Use a `select` setting for the icon (NOT inline_richtext — per research Open Question 1 recommendation). Provide 6 predefined icon names:
    - `natural` (circle-in-circle, maps to Natural Formula)
    - `shield` (shield path, maps to Cruelty-Free)
    - `checkmark` (checkmark-shield, maps to Expert Approved)
    - `shipping` (box/truck, maps to Free Shipping)
    - `leaf` (leaf SVG)
    - `star` (star SVG)

    Each block renders:
    ```liquid
    <div class="kanva-feature-strip__item">
      <div class="kanva-feature-strip__icon">
        {%- case block.settings.icon -%}
          {%- when 'natural' -%}
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2z"/><path d="M8 12c0-2.21 1.79-4 4-4s4 1.79 4 4-1.79 4-4 4-4-1.79-4-4z"/></svg>
          {%- when 'shield' -%}
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/></svg>
          {%- when 'checkmark' -%}
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z"/></svg>
          {%- when 'shipping' -%}
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><rect x="2" y="3" width="20" height="14" rx="2" ry="2"/><line x1="8" y1="21" x2="16" y2="21"/><line x1="12" y1="17" x2="12" y2="21"/></svg>
          {%- when 'leaf' -%}
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M17 8C8 10 5.9 16.17 3.82 21.34l1.89.66L12 14"/><path d="M2 2s7 4 14 1c0 0-1 7-8 10"/></svg>
          {%- when 'star' -%}
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5"><polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"/></svg>
        {%- endcase -%}
      </div>
      <div class="kanva-feature-strip__title">{{ block.settings.title }}</div>
      <div class="kanva-feature-strip__desc">{{ block.settings.description }}</div>
    </div>
    ```

    **Block schema settings:**
    - `icon` — select type with 6 options (natural, shield, checkmark, shipping, leaf, star), default "natural"
    - `title` — text type, default "Natural Formula"
    - `description` — text type, default "100% plant-based ingredients"

    **Section schema:**
    ```json
    {
      "name": "Kanva Feature Strip",
      "tag": "section",
      "disabled_on": { "groups": ["header"] },
      "settings": [
        { "type": "color_scheme", "id": "color_scheme", "label": "Color scheme", "default": "scheme-3" },
        { "type": "select", "id": "section_width", "label": "Section width", "options": [
          { "value": "page-width", "label": "Page width" },
          { "value": "full-width", "label": "Full width" }
        ], "default": "page-width" },
        { "type": "text", "id": "label", "label": "Section label" },
        { "type": "text", "id": "heading", "label": "Section heading" },
        { "type": "header", "content": "Padding" },
        { "type": "range", "id": "padding-block-start", "label": "Top", "min": 0, "max": 120, "step": 4, "unit": "px", "default": 80 },
        { "type": "range", "id": "padding-block-end", "label": "Bottom", "min": 0, "max": 120, "step": 4, "unit": "px", "default": 80 }
      ],
      "blocks": [
        { "type": "kanva-feature-item", "name": "Feature item" }
      ],
      "presets": [
        {
          "name": "Kanva Feature Strip",
          "category": "Kanva Editorial",
          "blocks": [
            { "type": "kanva-feature-item", "settings": { "icon": "natural", "title": "Natural Formula", "description": "100% plant-based ingredients" } },
            { "type": "kanva-feature-item", "settings": { "icon": "shield", "title": "Cruelty-Free", "description": "Never tested on animals" } },
            { "type": "kanva-feature-item", "settings": { "icon": "checkmark", "title": "Expert Approved", "description": "Dermatologist tested & approved" } },
            { "type": "kanva-feature-item", "settings": { "icon": "shipping", "title": "Free Shipping", "description": "On orders over €40" } }
          ]
        }
      ]
    }
    ```

    **`{% stylesheet %}` block** (section-scoped CSS):
    - `.kanva-feature-strip__grid`: display grid, grid-template-columns repeat(auto-fit, minmax(180px, 1fr)), gap 32px, text-align center, padding-top 32px
    - `.kanva-feature-strip__icon svg`: width 32px, height 32px, stroke var(--kanva-text-primary), margin-bottom 12px
    - `.kanva-feature-strip__title`: font-size 16px, font-weight 500, color var(--kanva-text-primary), margin-bottom 4px
    - `.kanva-feature-strip__desc`: font-size 14px, color var(--kanva-text-muted), line-height 1.5
    - `@media screen and (max-width: 749px)`: grid-template-columns repeat(2, 1fr), gap 24px

    NOTE: The background cream color comes from the `color-scheme-3` class (Horizon's warm scheme), NOT from a hardcoded `background: var(--kanva-cream)`. The `color_scheme` setting handles this.
  </action>
  <verify>
    <automated>cd /Users/mati/Mine/horizon && test -f sections/kanva-feature-strip.liquid && grep -q "kanva-feature-strip" sections/kanva-feature-strip.liquid && grep -q "kanva-feature-item" sections/kanva-feature-strip.liquid && grep -q "disabled_on" sections/kanva-feature-strip.liquid && grep -q "render 'kanva-heading'" sections/kanva-feature-strip.liquid && grep -q "render 'spacing-style'" sections/kanva-feature-strip.liquid && grep -q "Kanva Editorial" sections/kanva-feature-strip.liquid && sh scripts/phase1-regression-gate.sh && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - sections/kanva-feature-strip.liquid exists
    - File contains `render 'kanva-heading'` (uses shared heading snippet)
    - File contains `render 'spacing-style', settings: section.settings` (uses Horizon spacing helper)
    - File contains `"disabled_on": { "groups": ["header"] }` in schema
    - File contains `"type": "kanva-feature-item"` block definition
    - File contains `"category": "Kanva Editorial"` in presets
    - File contains 6 icon SVGs inside a case statement (natural, shield, checkmark, shipping, leaf, star)
    - File contains `"id": "padding-block-start"` and `"id": "padding-block-end"` range settings
    - File contains `"id": "color_scheme"` setting with default `"scheme-3"`
    - File contains `{% stylesheet %}` block with grid CSS
    - File contains `data-testid="kanva-feature-strip"`
    - `cd /Users/mati/Mine/horizon && sh scripts/phase1-regression-gate.sh` exits 0
  </acceptance_criteria>
  <done>Feature strip section exists with 6 predefined icons, block-based composition, Kanva heading integration, Horizon spacing/color scheme patterns, and preset with 4 default items. Regression gate passes.</done>
</task>

<task type="auto">
  <name>Task 2: Create kanva-testimonial.liquid section</name>
  <files>sections/kanva-testimonial.liquid</files>
  <read_first>
    - snippets/kanva-heading.liquid (verify render interface)
    - snippets/spacing-style.liquid (parameter interface)
    - theme-to-clone/kanva-components.html (lines 236-251 — testimonial HTML structure)
    - theme-to-clone/kanva-docs.md (Typography section — quote font sizes)
    - sections/media-with-content.liquid (lines 1-40 — Horizon section wrapper pattern)
    - .planning/phases/02-shared-kanva-primitives/02-RESEARCH.md (Testimonial Block Schema, Pitfall 2 — explicit block types)
  </read_first>
  <action>
    Create `sections/kanva-testimonial.liquid` — a centered quote testimonial section.

    **Liquid body structure:**
    1. `<div class="section-background color-{{ section.settings.color_scheme }}"></div>`
    2. Main wrapper: `<div class="section section--{{ section.settings.section_width }} color-{{ section.settings.color_scheme }} spacing-style kanva-testimonial" style="{% render 'spacing-style', settings: section.settings %}" data-testid="kanva-testimonial">`
    3. Optional heading via `{% render 'kanva-heading', label: section.settings.label, heading: section.settings.heading, alignment: 'center' %}`
    4. Testimonial content area: `<div class="kanva-testimonial__content">` containing `{%- content_for 'blocks' -%}`

    **Block type `kanva-testimonial-quote`:**

    Each block renders:
    ```liquid
    <div class="kanva-testimonial__quote-block" {{ block.shopify_attributes }}>
      <div class="kanva-testimonial__stars" aria-label="{{ block.settings.stars }} out of 5 stars">
        {%- for i in (1..block.settings.stars) -%}&#9733;{%- endfor -%}
      </div>
      <blockquote class="kanva-testimonial__quote">
        {{ block.settings.quote }}
      </blockquote>
      {%- if block.settings.author != blank -%}
        <cite class="kanva-testimonial__author">{{ block.settings.author }}</cite>
      {%- endif -%}
      {%- if block.settings.rating_summary != blank -%}
        <div class="kanva-testimonial__rating-summary">{{ block.settings.rating_summary }}</div>
      {%- endif -%}
    </div>
    ```

    **Block schema settings (per research Testimonial Block Schema):**
    - `quote` — textarea, label "Quote text"
    - `author` — text, label "Author name & title"
    - `stars` — range, min 1, max 5, step 1, default 5, label "Star rating"
    - `rating_summary` — text, label "Rating summary", default "4.7 / 5 · 1,109 reviews"

    **Section schema:**
    ```json
    {
      "name": "Kanva Testimonial",
      "tag": "section",
      "disabled_on": { "groups": ["header"] },
      "settings": [
        { "type": "color_scheme", "id": "color_scheme", "label": "Color scheme", "default": "scheme-3" },
        { "type": "select", "id": "section_width", "label": "Section width", "options": [
          { "value": "page-width", "label": "Page width" },
          { "value": "full-width", "label": "Full width" }
        ], "default": "page-width" },
        { "type": "text", "id": "label", "label": "Section label" },
        { "type": "text", "id": "heading", "label": "Section heading" },
        { "type": "header", "content": "Padding" },
        { "type": "range", "id": "padding-block-start", "label": "Top", "min": 0, "max": 120, "step": 4, "unit": "px", "default": 80 },
        { "type": "range", "id": "padding-block-end", "label": "Bottom", "min": 0, "max": 120, "step": 4, "unit": "px", "default": 80 }
      ],
      "blocks": [
        { "type": "kanva-testimonial-quote", "name": "Quote" }
      ],
      "presets": [
        {
          "name": "Kanva Testimonial",
          "category": "Kanva Editorial",
          "blocks": [
            { "type": "kanva-testimonial-quote", "settings": { "quote": "I've tried dozens of skincare brands and nothing has transformed my skin like Kanva. My skin is clearer, softer, and genuinely glowing.", "author": "Jennifer K., Verified Buyer", "stars": 5, "rating_summary": "4.7 / 5 · 1,109 reviews" } }
          ]
        }
      ]
    }
    ```

    **`{% stylesheet %}` block:**
    - `.kanva-testimonial__content`: text-align center, max-width 720px, margin 0 auto
    - `.kanva-testimonial__stars`: font-size 24px, color var(--kanva-sage), margin-bottom 24px, letter-spacing 4px
    - `.kanva-testimonial__quote`: font-size clamp(20px, 3vw, 28px), font-weight 400, line-height 1.5, color var(--kanva-text-primary), font-style normal, margin 0 0 24px, padding 0, border 0
    - `.kanva-testimonial__author`: font-size 14px, font-weight 500, color var(--kanva-text-primary), display block, margin-bottom 8px, font-style normal
    - `.kanva-testimonial__rating-summary`: font-size 13px, color var(--kanva-text-muted)

    Do NOT use `"type": "@theme"` in blocks — per Pitfall 2 from research, enumerate only the supported block types explicitly.
  </action>
  <verify>
    <automated>cd /Users/mati/Mine/horizon && test -f sections/kanva-testimonial.liquid && grep -q "kanva-testimonial" sections/kanva-testimonial.liquid && grep -q "kanva-testimonial-quote" sections/kanva-testimonial.liquid && grep -q "disabled_on" sections/kanva-testimonial.liquid && grep -q "render 'kanva-heading'" sections/kanva-testimonial.liquid && grep -q "render 'spacing-style'" sections/kanva-testimonial.liquid && grep -q "blockquote" sections/kanva-testimonial.liquid && sh scripts/phase1-regression-gate.sh && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - sections/kanva-testimonial.liquid exists
    - File contains `render 'kanva-heading'` (uses shared heading snippet)
    - File contains `render 'spacing-style', settings: section.settings` (uses Horizon spacing helper)
    - File contains `"disabled_on": { "groups": ["header"] }` in schema
    - File contains `"type": "kanva-testimonial-quote"` block definition (NOT `@theme`)
    - File contains `"category": "Kanva Editorial"` in presets
    - File contains `<blockquote` element for the quote
    - File contains `<cite` element for the author
    - File contains star rendering loop with `aria-label`
    - File contains `"id": "stars"` range setting with min 1, max 5
    - File contains `var(--kanva-sage)` for star color
    - File contains `data-testid="kanva-testimonial"`
    - `cd /Users/mati/Mine/horizon && sh scripts/phase1-regression-gate.sh` exits 0
  </acceptance_criteria>
  <done>Testimonial section exists with centered quote layout, configurable star rating, block-based quote composition, Kanva heading integration, and Horizon spacing/color scheme patterns. Regression gate passes.</done>
</task>

</tasks>

<verification>
1. Both section files exist in `sections/`
2. Both use `render 'kanva-heading'` and `render 'spacing-style'`
3. Both have `disabled_on: { groups: ["header"] }` in schema
4. Both have `presets` with `category: "Kanva Editorial"`
5. Feature strip has explicit `kanva-feature-item` block type with 6 icon options
6. Testimonial has explicit `kanva-testimonial-quote` block type with star rating
7. Neither section uses `@theme` for block types
8. `cd /Users/mati/Mine/horizon && sh scripts/phase1-regression-gate.sh` exits 0
</verification>

<success_criteria>
- Feature strip section renders a 4-column icon grid with 6 predefined SVG icons
- Testimonial section renders centered quote with configurable stars and author attribution
- Both sections integrate with Kanva heading snippet and Horizon spacing/color infrastructure
- Both sections are available in the "Kanva Editorial" category in the theme editor
- Phase 1 regression gate passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/02-shared-kanva-primitives/02-02-SUMMARY.md`
</output>
