---
phase: 03-landing-page-composition
plan: "03"
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - templates/index.json
autonomous: true
requirements: [HOME-01, HOME-02, HOME-03, HOME-04]

must_haves:
  truths:
    - "Shopper lands on a fully composed Kanva homepage with hero, trust strip, marquee, product showcasing, eco story, bento grid, testimonials, newsletter, and social proof"
    - "Merchant sees all sections pre-wired with sensible defaults on first template load"
    - "Section order matches D-16: hero, feature-strip, marquee, product-list x2, media-with-content eco, bento-grid, testimonial, newsletter, image-grid"
    - "Product-list sections link to real Shopify collection references"
    - "Media-with-content eco section uses correct static block IDs for Horizon's content_for system"
  artifacts:
    - path: "templates/index.json"
      provides: "Complete Kanva homepage template composition"
      contains: "kanva-hero"
  key_links:
    - from: "templates/index.json"
      to: "sections/kanva-hero.liquid"
      via: "type reference in section entry"
      pattern: "\"type\": \"kanva-hero\""
    - from: "templates/index.json"
      to: "sections/kanva-feature-strip.liquid"
      via: "type reference in section entry"
      pattern: "\"type\": \"kanva-feature-strip\""
    - from: "templates/index.json"
      to: "sections/kanva-marquee.liquid"
      via: "type reference in section entry"
      pattern: "\"type\": \"kanva-marquee\""
    - from: "templates/index.json"
      to: "sections/product-list.liquid"
      via: "type reference in section entry"
      pattern: "\"type\": \"product-list\""
    - from: "templates/index.json"
      to: "sections/media-with-content.liquid"
      via: "type reference with static blocks"
      pattern: "\"type\": \"media-with-content\""
    - from: "templates/index.json"
      to: "sections/kanva-bento-grid.liquid"
      via: "type reference in section entry"
      pattern: "\"type\": \"kanva-bento-grid\""
    - from: "templates/index.json"
      to: "sections/kanva-testimonial.liquid"
      via: "type reference in section entry"
      pattern: "\"type\": \"kanva-testimonial\""
    - from: "templates/index.json"
      to: "sections/kanva-newsletter.liquid"
      via: "type reference in section entry"
      pattern: "\"type\": \"kanva-newsletter\""
    - from: "templates/index.json"
      to: "sections/kanva-image-grid.liquid"
      via: "type reference in section entry"
      pattern: "\"type\": \"kanva-image-grid\""
---

<objective>
Compose the complete `templates/index.json` that wires all 10 homepage sections into the prescribed Kanva order with sensible defaults. This replaces the current two-section index template.

Purpose: D-15 requires a complete homepage template pre-wired so the merchant sees the full Kanva homepage on first load. D-16 prescribes the exact section order. This is the integration point that ties all Phase 2 primitives and Phase 3 new sections into a cohesive homepage experience, fulfilling HOME-01 through HOME-04.
Output: `templates/index.json` (complete replacement)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-landing-page-composition/03-CONTEXT.md
@.planning/phases/03-landing-page-composition/03-RESEARCH.md
@.planning/phases/03-landing-page-composition/03-01-SUMMARY.md
@.planning/phases/03-landing-page-composition/03-02-SUMMARY.md

<interfaces>
<!-- The executor needs the current index.json format and all section schemas to compose the template correctly. -->

Current templates/index.json structure (will be fully replaced):
```json
{
  "sections": {
    "hero_jVaWmY": { "type": "hero", ... },
    "product_list_fa6P9H": { "type": "product-list", ... }
  },
  "order": ["hero_jVaWmY", "product_list_fa6P9H"]
}
```

Existing product-list static block pattern (from current index.json):
```json
"blocks": {
  "static-header": {
    "type": "_product-list-content",
    "name": "t:names.header",
    "static": true,
    "settings": { ... },
    "blocks": {
      "product_list_text_XXXXXX": { "type": "_product-list-text", ... },
      "product_list_button_XXXXXX": { "type": "_product-list-button", ... }
    },
    "block_order": ["product_list_text_XXXXXX", "product_list_button_XXXXXX"]
  },
  "static-product-card": {
    "type": "_product-card",
    "name": "t:names.product_card",
    "static": true,
    "settings": { ... },
    "blocks": { ... }
  }
}
```

media-with-content static block IDs (from section source):
```liquid
{% content_for 'block', type: '_media-without-appearance', id: 'media' %}
{% content_for 'block', type: '_content-without-appearance', id: 'content' %}
```
So index.json entry needs blocks keyed as `"media"` and `"content"` with `"static": true`.

Section order per D-16:
1. kanva-hero
2. kanva-feature-strip
3. kanva-marquee
4. product-list (cleansers)
5. product-list (lotions)
6. media-with-content (eco story row)
7. kanva-bento-grid
8. kanva-testimonial
9. kanva-newsletter
10. kanva-image-grid
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compose complete templates/index.json with all 10 Kanva homepage sections</name>
  <files>templates/index.json</files>
  <read_first>
    - templates/index.json (current file — understand the auto-generated header comment pattern, existing product-list static block structure for replication)
    - sections/kanva-hero.liquid (read schema section to get exact setting IDs for hero entry defaults)
    - sections/kanva-feature-strip.liquid (read schema section to get exact setting IDs)
    - sections/kanva-marquee.liquid (read schema section to get exact setting IDs)
    - sections/product-list.liquid (read schema section to understand static block types and IDs: _product-list-content with id static-header, _product-card with id static-product-card)
    - sections/media-with-content.liquid (read schema to confirm static block types: _media-without-appearance id media, _content-without-appearance id content)
    - sections/kanva-bento-grid.liquid (read schema to get block types and setting IDs)
    - sections/kanva-testimonial.liquid (read schema to get setting IDs)
    - sections/kanva-newsletter.liquid (read schema to get setting IDs)
    - sections/kanva-image-grid.liquid (read schema to get setting IDs and block structure)
  </read_first>
  <action>
    Write a complete `templates/index.json` that replaces the current file. Per D-15, all sections pre-wired with sensible defaults. Per D-16, the prescribed section order.

    **File structure rules:**
    - Retain the auto-generated header comment block from the current file (lines 1-9 of existing index.json)
    - Top-level keys: `"sections"` (object) and `"order"` (array of section IDs)
    - Each section ID uses `snake_case_suffix` format with unique 6-char alphanumeric suffixes (per Pitfall 4 — NO duplicate suffixes across sections or blocks)
    - Block IDs within each section also use unique suffixes

    **Section entries (10 total, in order):**

    **1. kanva-hero** (`kanva_hero_Kh3xR7`):
    - `"type": "kanva-hero"`
    - 3 slide blocks (kanva-slide type) with placeholder content:
      - Slide 1: heading_line1 "Natural", heading_line2 "Skincare", subtitle "Start your day with gentle care and nourishing ingredients.", cta_label "Shop Now", cta_url "shopify://collections/all", media_type_1 "image"
      - Slide 2: heading_line1 "Pure", heading_line2 "Ingredients", subtitle "Ethically sourced botanicals for radiant skin.", cta_label "Explore", cta_url "shopify://collections/all", media_type_1 "image"
      - Slide 3: heading_line1 "Gentle", heading_line2 "Formulas", subtitle "Dermatologist-tested care for every skin type.", cta_label "Discover", cta_url "shopify://collections/all", media_type_1 "image"
    - Section settings: color_scheme "scheme-6", section_width "full-width", show_preview_rail true, preview_collection "shopify://collections/all", padding-block-start 0, padding-block-end 0

    **2. kanva-feature-strip** (`kanva_feature_strip_Fs4wQ1`):
    - `"type": "kanva-feature-strip"`
    - 4 feature item blocks with: (natural, "100% Natural"), (shield, "Dermatologist Tested"), (leaf, "Eco Friendly"), (shipping, "Free Shipping")
    - Section settings: color_scheme "scheme-3", section_width "page-width", padding-block-start 40, padding-block-end 40

    **3. kanva-marquee** (`kanva_marquee_Mq8nT5`):
    - `"type": "kanva-marquee"`
    - No blocks (phrases in section settings)
    - Settings: color_scheme "scheme-1", phrase_1 "Natural Ingredients", phrase_2 "Dermatologist Approved", phrase_3 "Cruelty Free", separator "🌿", padding-block-start 20, padding-block-end 20

    **4. product-list cleansers** (`product_list_cleansers_Pc2kL9`):
    - `"type": "product-list"`
    - Copy the full static block structure from the existing index.json product_list entry (static-header with _product-list-content containing _product-list-text + _product-list-button, static-product-card with _product-card containing _product-card-gallery + product-title + price). Use NEW unique suffixes for all nested block IDs.
    - Settings: collection "all", layout_type "grid", max_products 3, columns 3, mobile_columns "2", columns_gap 8, rows_gap 24, section_width "page-width", color_scheme "scheme-1", padding-block-start 48, padding-block-end 48

    **5. product-list lotions** (`product_list_lotions_Pl6mY3`):
    - `"type": "product-list"`
    - Same static block structure as above with DIFFERENT unique suffixes
    - Same settings as cleansers (collection "all", max_products 3, columns 3, etc.)

    **6. media-with-content eco** (`media_with_content_eco_Me7jH2`):
    - `"type": "media-with-content"`
    - Static blocks: `"media": { "type": "_media-without-appearance", "static": true }` and `"content": { "type": "_content-without-appearance", "static": true, "settings": { "horizontal_alignment_flex_direction_column": "flex-start", "vertical_alignment_flex_direction_column": "space-between" }, "blocks": { ... } }`
    - Inside content block: 3 text blocks for eco checklist items (e.g., "Sustainable sourcing practices", "Recyclable packaging materials", "Carbon neutral shipping")
    - Section settings: media_position "right", color_scheme "scheme-3", media_width "medium", media_height "auto", section_width "page-width", padding-block-start 80, padding-block-end 80

    **7. kanva-bento-grid** (`kanva_bento_grid_Bg5pN8`):
    - `"type": "kanva-bento-grid"`
    - 7 blocks matching the preset from 03-02 plan: title (col_span "2"), image, rating, image (row_span true), text (eco), navy, text (proven, col_span "2", show_checkmark true)
    - Settings: color_scheme "scheme-1", section_width "page-width", heading "Why Choose Us", padding-block-start 80, padding-block-end 80

    **8. kanva-testimonial** (`kanva_testimonial_Kt9dW4`):
    - `"type": "kanva-testimonial"`
    - Read section schema to determine correct block type name and settings. Wire 3 testimonial blocks with placeholder quotes.
    - Settings: color_scheme "scheme-3", section_width "page-width", heading "What Our Customers Say", padding-block-start 80, padding-block-end 80

    **9. kanva-newsletter** (`kanva_newsletter_Kn1vB6`):
    - `"type": "kanva-newsletter"`
    - Read section schema to determine settings. No blocks (form is section-level).
    - Settings: color_scheme "scheme-3", section_width "page-width", heading "Stay Connected", padding-block-start 80, padding-block-end 80

    **10. kanva-image-grid** (`kanva_image_grid_Ki3fA0`):
    - `"type": "kanva-image-grid"`
    - Read section schema to determine block type name. Wire 6 image blocks with empty image_picker defaults (merchant will add images).
    - Settings: color_scheme "scheme-1", section_width "page-width", heading "Follow Our Journey", padding-block-start 80, padding-block-end 80

    **Order array:**
    ```json
    "order": [
      "kanva_hero_Kh3xR7",
      "kanva_feature_strip_Fs4wQ1",
      "kanva_marquee_Mq8nT5",
      "product_list_cleansers_Pc2kL9",
      "product_list_lotions_Pl6mY3",
      "media_with_content_eco_Me7jH2",
      "kanva_bento_grid_Bg5pN8",
      "kanva_testimonial_Kt9dW4",
      "kanva_newsletter_Kn1vB6",
      "kanva_image_grid_Ki3fA0"
    ]
    ```

    **CRITICAL:** After writing the file, read it back to verify valid JSON (no trailing commas, no duplicate keys). All section IDs in `"order"` must exactly match section keys in `"sections"`. All block IDs in `"block_order"` arrays must exactly match block keys in their parent `"blocks"` objects. Use `node -e "JSON.parse(require('fs').readFileSync('templates/index.json','utf8'));console.log('Valid JSON')"` to verify.
  </action>
  <verify>
    <automated>cd /Users/mati/Mine/horizon && node -e "const j=JSON.parse(require('fs').readFileSync('templates/index.json','utf8'));console.log('sections:',Object.keys(j.sections).length,'order:',j.order.length);console.log('types:',Object.values(j.sections).map(s=>s.type).join(', '))" && grep -c "kanva-hero" templates/index.json && grep -c "kanva-feature-strip" templates/index.json && grep -c "kanva-marquee" templates/index.json && grep -c "product-list" templates/index.json && grep -c "media-with-content" templates/index.json && grep -c "kanva-bento-grid" templates/index.json && grep -c "kanva-testimonial" templates/index.json && grep -c "kanva-newsletter" templates/index.json && grep -c "kanva-image-grid" templates/index.json</automated>
  </verify>
  <acceptance_criteria>
    - templates/index.json is valid JSON (node JSON.parse succeeds)
    - File contains exactly 10 section entries in the sections object
    - File contains exactly 10 entries in the order array
    - All 10 section IDs in order array match keys in sections object
    - Section types present: "kanva-hero", "kanva-feature-strip", "kanva-marquee", "product-list" (x2), "media-with-content", "kanva-bento-grid", "kanva-testimonial", "kanva-newsletter", "kanva-image-grid"
    - Order matches D-16: hero first, feature-strip second, marquee third, product-list x2 next, media-with-content next, bento-grid, testimonial, newsletter, image-grid last
    - kanva-hero entry has 3 slide blocks with heading_line1, heading_line2, cta_label settings
    - product-list entries have static-header and static-product-card blocks with "static": true
    - media-with-content entry has "media" and "content" blocks with "static": true
    - kanva-bento-grid entry has 7 blocks
    - No duplicate section IDs or block IDs anywhere in the file
    - All section IDs use unique suffixes (no two share the same 6-char suffix)
    - kanva-hero settings include show_preview_rail: true
    - product-list settings include max_products: 3, columns: 3
  </acceptance_criteria>
  <done>templates/index.json fully replaced with 10-section Kanva homepage composition in the prescribed D-16 order, all sections pre-wired with sensible defaults, valid JSON, unique IDs throughout</done>
</task>

</tasks>

<verification>
1. `node -e "JSON.parse(require('fs').readFileSync('templates/index.json','utf8'));console.log('Valid')"` succeeds
2. `shopify theme check --fail-level error` passes (allowing only the 2 pre-existing header baseline errors)
3. The order array lists exactly 10 section IDs matching the D-16 prescribed order
4. All section types resolve to existing .liquid files in the sections/ directory
</verification>

<success_criteria>
- templates/index.json is valid JSON with 10 sections in D-16 order
- Hero has 3 pre-wired slides with carousel content
- Product-list sections use correct static block pattern from Horizon
- Media-with-content eco section uses correct static block IDs (media + content)
- Bento grid has 7 blocks matching reference layout
- All section settings use Kanva defaults (scheme-3 cream, scheme-6 dark hero, etc.)
- Theme check passes without new errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-landing-page-composition/03-03-SUMMARY.md`
</output>
