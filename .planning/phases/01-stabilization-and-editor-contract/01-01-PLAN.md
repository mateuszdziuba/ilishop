---
phase: 01-stabilization-and-editor-contract
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-stabilization-and-editor-contract/01-responsive-editor-contract.md
  - snippets/kanva-responsive-media.liquid
  - sections/hero.liquid
autonomous: true
requirements:
  - EDIT-02
  - EDIT-03
  - EDIT-05
  - QUAL-03
must_haves:
  truths:
    - Merchant-facing responsive settings use one desktop-default plus optional mobile-override contract instead of duplicate content trees.
    - Supported editorial sections have a fixed vocabulary for mobile media and layout-position overrides with predictable desktop fallback behavior.
    - The contract is proven in live theme code without breaking hero editor behavior or saved setting IDs.
  artifacts:
    - path: .planning/phases/01-stabilization-and-editor-contract/01-responsive-editor-contract.md
      provides: Canonical responsive settings contract, fallback matrix, and Horizon primitive inventory for later Kanva sections.
    - path: snippets/kanva-responsive-media.liquid
      provides: Shared media-rendering helper for desktop-first mobile override behavior.
    - path: sections/hero.liquid
      provides: Existing Horizon hero updated to consume the shared contract helper without changing merchant-facing schema IDs.
  key_links:
    - from: .planning/phases/01-stabilization-and-editor-contract/01-responsive-editor-contract.md
      to: sections/hero.liquid
      via: Canonical responsive setting IDs and fallback rules
      pattern: custom_mobile_media|stack_media_on_mobile|image_1_mobile|video_1_mobile
    - from: sections/hero.liquid
      to: snippets/kanva-responsive-media.liquid
      via: render call for shared media fallback behavior
      pattern: render 'kanva-responsive-media'
---

<objective>
Freeze the Phase 1 responsive editor contract and prove it through the smallest reusable implementation surface.

Purpose: Lock the desktop-first mobile-override model early so later Kanva sections cannot drift into duplicate content trees or ad hoc setting names.
Output: A contract artifact for downstream phases plus one shared responsive-media helper integrated into the existing hero pattern.
</objective>

<execution_context>
@/Users/mati/.codex/get-shit-done/workflows/execute-plan.md
@/Users/mati/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-stabilization-and-editor-contract/01-CONTEXT.md
@.planning/phases/01-stabilization-and-editor-contract/01-RESEARCH.md
@.planning/codebase/CONCERNS.md
@sections/hero.liquid
@sections/section.liquid
@snippets/section.liquid
@snippets/background-media.liquid

<interfaces>
From `sections/hero.liquid`:

```liquid
if section.settings.custom_mobile_media == false or media_count_mobile == 0 or media_count == 0
  assign media_count_mobile = media_count
  assign fallback_to_desktop = true
endif
```

```liquid
"id": "custom_mobile_media"
"id": "stack_media_on_mobile"
"id": "media_type_1_mobile"
"id": "image_1_mobile"
"id": "video_1_mobile"
```

From `sections/section.liquid`:

```liquid
"id": "vertical_on_mobile"
"id": "horizontal_alignment"
"id": "vertical_alignment"
"id": "background_media"
"id": "video_position"
"id": "background_image_position"
```

From `snippets/section.liquid`:

```liquid
{% render 'background-media',
  background_media: section.settings.background_media,
  background_video: section.settings.video,
  background_video_position: section.settings.video_position,
  background_image: section.settings.background_image,
  background_image_position: section.settings.background_image_position
%}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the canonical responsive editor contract</name>
  <files>.planning/phases/01-stabilization-and-editor-contract/01-responsive-editor-contract.md</files>
  <read_first>
    .planning/phases/01-stabilization-and-editor-contract/01-responsive-editor-contract.md
    .planning/phases/01-stabilization-and-editor-contract/01-CONTEXT.md
    .planning/phases/01-stabilization-and-editor-contract/01-RESEARCH.md
    sections/hero.liquid
    sections/section.liquid
    snippets/section.liquid
  </read_first>
  <acceptance_criteria>
    `rg -n "D-01|D-05|D-08|D-14|desktop default|mobile override|no duplicate content trees" .planning/phases/01-stabilization-and-editor-contract/01-responsive-editor-contract.md` returns matches.
    `rg -n "custom_mobile_media|stack_media_on_mobile|vertical_on_mobile|horizontal_alignment|vertical_alignment|background_media|background_image_position|video_position" .planning/phases/01-stabilization-and-editor-contract/01-responsive-editor-contract.md` returns matches.
    The document contains a table or bullet matrix that names the allowed override categories, fallback behavior, and the Horizon primitives later phases must reuse first.
  </acceptance_criteria>
  <action>Create `.planning/phases/01-stabilization-and-editor-contract/01-responsive-editor-contract.md` as the source-of-truth contract for later Kanva sections. Encode locked decisions D-01 through D-08 and D-13 through D-15 explicitly: desktop media is canonical, mobile media is optional override only, text/CTA/semantic structure stay shared, and later sections may only add mobile overrides for media, position, and stack behavior where layout materially changes. Reuse the existing Horizon setting language from `sections/hero.liquid` and `sections/section.liquid` instead of inventing new IDs. Include a concrete setting-ID matrix, a fallback matrix covering blank mobile media and disabled mobile overrides, an anti-drift section banning duplicate breakpoint content trees, and an inventory of the exact Horizon primitives/snippets later phases must reuse first.</action>
  <verify>
    <automated>rg -n "custom_mobile_media|stack_media_on_mobile|vertical_on_mobile|background_media|desktop default|mobile override" .planning/phases/01-stabilization-and-editor-contract/01-responsive-editor-contract.md</automated>
  </verify>
  <done>The contract document exists and a downstream executor can derive the exact allowed responsive settings, fallback rules, and Horizon reuse boundary without rereading all Phase 1 research.</done>
</task>

<task type="auto">
  <name>Task 2: Extract the minimal responsive-media helper and prove it in hero</name>
  <files>snippets/kanva-responsive-media.liquid, sections/hero.liquid</files>
  <read_first>
    snippets/kanva-responsive-media.liquid
    sections/hero.liquid
    snippets/background-media.liquid
    .planning/phases/01-stabilization-and-editor-contract/01-responsive-editor-contract.md
    .planning/phases/01-stabilization-and-editor-contract/01-RESEARCH.md
  </read_first>
  <acceptance_criteria>
    `rg -n "render 'kanva-responsive-media'" sections/hero.liquid` returns at least one match.
    `rg -n "custom_mobile_media|stack_media_on_mobile|media_type_1_mobile|image_1_mobile|video_1_mobile" sections/hero.liquid` still returns matches so merchant-facing schema IDs remain intact.
    `shopify theme check --fail-level error` exits successfully after the helper extraction.
  </acceptance_criteria>
  <action>Create `snippets/kanva-responsive-media.liquid` as the smallest reusable rendering helper that later editorial sections can call for desktop-first media with optional mobile override. The helper should accept desktop and mobile image/video inputs plus wrapper classes and render the same outcome the hero currently supports: desktop media always exists when configured, mobile media is opt-in, blank mobile media falls back to desktop media, and no duplicate text/content tree is introduced. Then update `sections/hero.liquid` to delegate the media-slot rendering branches to the new helper while preserving the current schema setting IDs, `visible_if` behavior, responsive classes, and existing `data-testid` hooks. Keep the refactor narrow: do not rename hero settings, do not touch unrelated hero content blocks, and do not refactor slideshow or global section wrapper code in this plan.</action>
  <verify>
    <automated>shopify theme check --fail-level error</automated>
  </verify>
  <done>`sections/hero.liquid` proves the shared contract in real runtime code, and the new helper becomes the reusable baseline for later Kanva sections without widening Phase 1 into a broad refactor.</done>
</task>

</tasks>

<verification>
Run `shopify theme check --fail-level error`.
Read the contract artifact and confirm it names the canonical responsive setting IDs and fallback rules from `hero` and `section`.
Confirm `sections/hero.liquid` now renders through `snippets/kanva-responsive-media.liquid` while keeping the existing merchant schema surface.
</verification>

<success_criteria>
Phase 1 now has one enforceable responsive-settings contract, one reusable media helper, and one live Horizon section proving the contract without introducing duplicate breakpoint content or schema drift.
</success_criteria>

<output>
After completion, create `.planning/phases/01-stabilization-and-editor-contract/01-01-SUMMARY.md`
</output>
