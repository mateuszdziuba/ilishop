<div class="section-background color-{{ section.settings.color_scheme }}"></div>
<div
  class="section section--{{ section.settings.section_width }} color-{{ section.settings.color_scheme }} spacing-style ili-newsletter"
  style="{% render 'spacing-style', settings: section.settings %}"
  data-testid="ili-newsletter"
>
  <div class="ili-newsletter__inner">
    <div class="ili-newsletter__content">
      {% render 'ili-heading',
        label: section.settings.label,
        heading: section.settings.heading,
        subtext: section.settings.subtext,
        heading_level: 'h2'
      %}
      {%- comment -%} CRITICAL: Use 'customer' form, NOT 'contact' — native Shopify newsletter subscription {%- endcomment -%}
      {% form 'customer', id: 'ili-newsletter-' | append: section.id %}
        {%- if form.posted_successfully? -%}
          <p class="ili-newsletter__success">{{ section.settings.success_message | default: 'Thank you for subscribing!' }}</p>
        {%- else -%}
          {%- if form.errors -%}
            <p class="ili-newsletter__error">{{ form.errors.translated_fields['email'] | default: 'Please enter a valid email.' }}</p>
          {%- endif -%}
          <input type="hidden" name="contact[tags]" value="newsletter">
          <div class="ili-newsletter__form">
            <input
              type="email"
              name="contact[email]"
              id="ili-newsletter-email-{{ section.id }}"
              placeholder="{{ section.settings.email_placeholder | default: 'Your email address' }}"
              required
              class="ili-newsletter__input"
              autocomplete="email"
            >
            <button type="submit" class="ili-newsletter__btn">
              {{ section.settings.button_label | default: 'Subscribe' }}
            </button>
          </div>
        {%- endif -%}
      {% endform %}
    </div>

    {%- if section.settings.image != blank -%}
      <div class="ili-newsletter__image">
        {{ section.settings.image | image_url: width: 600 | image_tag: alt: section.settings.image.alt, loading: 'lazy', class: 'ili-newsletter__img' }}
      </div>
    {%- endif -%}
  </div>
</div>

{% stylesheet %}
  .ili-newsletter__inner {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 48px;
    align-items: center;
  }

  .ili-newsletter__form {
    display: flex;
    gap: 0;
    margin-top: 24px;
  }

  .ili-newsletter__input {
    flex: 1;
    padding: 14px 20px;
    border: 1px solid var(--ili-border);
    border-radius: var(--ili-btn-radius) 0 0 var(--ili-btn-radius);
    font-size: 15px;
    outline: none;
    background: #fff;
  }

  .ili-newsletter__input:focus {
    border-color: var(--ili-text-primary);
  }

  .ili-newsletter__btn {
    padding: 14px 28px;
    background: var(--ili-text-primary);
    color: #fff;
    border: 0;
    border-radius: 0 var(--ili-btn-radius) var(--ili-btn-radius) 0;
    font-size: 14px;
    font-weight: 500;
    cursor: pointer;
    white-space: nowrap;
  }

  .ili-newsletter__btn:hover {
    opacity: 0.85;
  }

  .ili-newsletter__img {
    width: 100%;
    border-radius: var(--ili-card-radius);
    object-fit: cover;
    max-height: 400px;
  }

  .ili-newsletter__success {
    color: var(--ili-sage);
    font-weight: 500;
    margin-top: 16px;
  }

  .ili-newsletter__error {
    color: #c0392b;
    font-size: 14px;
    margin-bottom: 8px;
  }

  @media screen and (max-width: 749px) {
    .ili-newsletter__inner {
      grid-template-columns: 1fr;
    }

    .ili-newsletter__image {
      order: -1;
    }
  }
{% endstylesheet %}

{% schema %}
{
  "name": "Ili Newsletter",
  "tag": "section",
  "disabled_on": {
    "groups": ["header"]
  },
  "settings": [
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "Color scheme",
      "default": "scheme-3"
    },
    {
      "type": "select",
      "id": "section_width",
      "label": "Section width",
      "options": [
        { "value": "page-width", "label": "Page width" },
        { "value": "full-width", "label": "Full width" }
      ],
      "default": "page-width"
    },
    {
      "type": "text",
      "id": "label",
      "label": "Section label"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Stay Updated, Stay Radiant"
    },
    {
      "type": "textarea",
      "id": "subtext",
      "label": "Subtext",
      "default": "Get skincare tips, new arrivals, and exclusive offers delivered to your inbox."
    },
    {
      "type": "text",
      "id": "email_placeholder",
      "label": "Email placeholder",
      "default": "Your email address"
    },
    {
      "type": "text",
      "id": "button_label",
      "label": "Button label",
      "default": "Subscribe"
    },
    {
      "type": "text",
      "id": "success_message",
      "label": "Success message",
      "default": "Thank you for subscribing!"
    },
    {
      "type": "image_picker",
      "id": "image",
      "label": "Decorative image"
    },
    {
      "type": "header",
      "content": "Padding"
    },
    {
      "type": "range",
      "id": "padding-block-start",
      "label": "Top",
      "min": 0,
      "max": 120,
      "step": 4,
      "unit": "px",
      "default": 80
    },
    {
      "type": "range",
      "id": "padding-block-end",
      "label": "Bottom",
      "min": 0,
      "max": 120,
      "step": 4,
      "unit": "px",
      "default": 80
    }
  ],
  "blocks": [],
  "presets": [
    {
      "name": "Ili Newsletter",
      "category": "Ili Editorial"
    }
  ]
}
{% endschema %}
