<div class="section-background color-{{ section.settings.color_scheme }}"></div>
<div
  class="section section--{{ section.settings.section_width }} color-{{ section.settings.color_scheme }} spacing-style kanva-image-grid"
  style="{% render 'spacing-style', settings: section.settings %}"
  data-testid="kanva-image-grid"
>
  <div class="kanva-image-grid__header">
    <div class="kanva-image-grid__heading">
      {% render 'kanva-heading', label: section.settings.label, heading: section.settings.heading %}
    </div>
    {%- if section.settings.link_text != blank -%}
      <a href="{{ section.settings.link_url }}" class="kanva-image-grid__link">{{ section.settings.link_text }}</a>
    {%- endif -%}
  </div>

  <div class="kanva-image-grid__grid">
    {%- comment -%} Each kanva-image-grid-item block renders via blocks/kanva-image-grid-item.liquid using: render 'kanva-card' {%- endcomment -%}
    {%- content_for 'blocks' -%}
  </div>
</div>

{% stylesheet %}
  .kanva-image-grid__header {
    display: flex;
    justify-content: space-between;
    align-items: flex-end;
    margin-bottom: 32px;
    flex-wrap: wrap;
    gap: 16px;
  }

  .kanva-image-grid__link {
    font-size: 14px;
    font-weight: 500;
    color: var(--kanva-text-primary);
    text-decoration: none;
    border-bottom: 1px solid currentColor;
  }

  .kanva-image-grid__link:hover {
    opacity: 0.7;
  }

  .kanva-image-grid__grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(180px, 1fr));
    gap: 4px;
  }

  .kanva-image-grid__item {
    aspect-ratio: 1 / 1;
  }

  @media screen and (max-width: 749px) {
    .kanva-image-grid__grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }

  @media screen and (max-width: 449px) {
    .kanva-image-grid__grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }
{% endstylesheet %}

{% schema %}
{
  "name": "Kanva Image Grid",
  "tag": "section",
  "disabled_on": {
    "groups": ["header"]
  },
  "settings": [
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "Color scheme",
      "default": "scheme-1"
    },
    {
      "type": "select",
      "id": "section_width",
      "label": "Section width",
      "options": [
        { "value": "page-width", "label": "Page width" },
        { "value": "full-width", "label": "Full width" }
      ],
      "default": "page-width"
    },
    {
      "type": "text",
      "id": "label",
      "label": "Section label",
      "default": "Follow us on Instagram"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Section heading"
    },
    {
      "type": "text",
      "id": "link_text",
      "label": "Link text",
      "default": "@kanva"
    },
    {
      "type": "url",
      "id": "link_url",
      "label": "Link URL"
    },
    {
      "type": "header",
      "content": "Padding"
    },
    {
      "type": "range",
      "id": "padding-block-start",
      "label": "Top",
      "min": 0,
      "max": 120,
      "step": 4,
      "unit": "px",
      "default": 80
    },
    {
      "type": "range",
      "id": "padding-block-end",
      "label": "Bottom",
      "min": 0,
      "max": 120,
      "step": 4,
      "unit": "px",
      "default": 80
    }
  ],
  "blocks": [
    {
      "type": "kanva-image-grid-item",
      "name": "Image"
    }
  ],
  "presets": [
    {
      "name": "Kanva Image Grid",
      "category": "Kanva Editorial",
      "blocks": [
        { "type": "kanva-image-grid-item" },
        { "type": "kanva-image-grid-item" },
        { "type": "kanva-image-grid-item" },
        { "type": "kanva-image-grid-item" },
        { "type": "kanva-image-grid-item" },
        { "type": "kanva-image-grid-item" }
      ]
    }
  ]
}
{% endschema %}
